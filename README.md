# To run the code 
```bash
# create the virtual environment
python -m venv env

source env/bin/activate

pip install -r requirements.txt


# postgres db docker setup
# write here the steps to setup 

# run the backend server
uvicorn main:app --reload
```
